# Only the `sensor_cfg` subpackage is used in this release (the SharpaVBTS sensor
# cfg referenced by the base env). The VBTS in-hand-rotate task and its agents are
# not shipped, so there is no gym registration / agents import here.
